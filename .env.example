# Copy to .env.local (never commit .env.local)
# Frequency Supabase — Dyzen Solar CRM (LAN staging until Phase D)

VITE_SUPABASE_URL=http://192.168.1.69:8000
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional server-only (API routes / scripts — never expose to the browser)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# DB schema used by the JS client (see src/supabase.js)
# VITE_SUPABASE_SCHEMA=dyzen
